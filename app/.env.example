# ============ Upbit Store — frontend env ============
# Salin ke app/.env untuk lokal, dan set yang sama di Vercel.

# Google OAuth Client ID (tipe Web). Kosong = tombol Google mode demo.
VITE_GOOGLE_CLIENT_ID="xxxxxxxx.apps.googleusercontent.com"

# Base URL API. Kosongkan di Vercel (frontend & API satu domain).
# VITE_API_BASE=""
